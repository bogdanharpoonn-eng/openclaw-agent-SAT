# Railway: EU West region set in UI (Settings > Scale > Regions).
# No Dockerfile in repo root -> Railpack build (more reliable healthchecks).
[build]
builder = "RAILPACK"

[deploy]
startCommand = "node index.js"
restartPolicyType = "ON_FAILURE"
